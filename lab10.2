#include <stdio.h>

// Задание 3

int main()
{
    char str[31];
    int i, slv = 0;
    printf("Vvedite stroky\n");
    gets(str);
    for (i = 0; i < 31; i++)
    {
        if (str[i] == 'a')
        {
            if (str[i+1] == ' ') slv++;
                else if (str[i+1] == '\0') slv++;
        }
    }
    printf("vsego slov: %d", slv);
    
    
    
    return 0;
}
